[package]
name = "arrow-deps"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
# Crates.io dependencies
arrow-flight = "7"

# Git dependencies
datafusion = { git = "https://github.com/apache/arrow-datafusion", rev = "15af24ab3d21c6baa3a7fff191ceecafce20dd35" }
# deltalake = { git = "https://github.com/roeap/delta-rs", branch = "wb", features = [
#   "datafusion-ext",
# ], optional = true }
deltalake = { path = "../../../delta-rs/rust", optional = true, default-features = false, features = [
  "datafusion-ext",
] }

[features]
delta = ["deltalake"]
